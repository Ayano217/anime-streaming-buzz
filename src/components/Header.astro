---
const currentPath = Astro.url.pathname;

const navItems = [
  { label: 'Home', href: '/' },
  { label: 'Browse', href: '/browse' },
  { label: 'Manga', href: '/category/manga' },
  { label: 'News', href: '/category/news' },
  { label: 'Streaming', href: '/category/streaming' },
];
---

<header class="site-header" id="siteHeader">
  <div class="container header-inner">
    
    <a href="/" class="header-logo">
      <span class="grad-text">Ani</span><span class="tube-text">Tube</span><span class="grad-text">Buzz</span>
    </a>
    
    <nav class="header-nav" aria-label="Main navigation">
      {navItems.map((item) => (
        <a 
          href={item.href} 
          class={`nav-link ${currentPath === item.href ? 'active' : ''}`}
        >
          {item.label}
        </a>
      ))}
    </nav>
    
    <div class="header-right">
      
      <div class="search-inline" id="searchWrap">
        <svg class="si-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
          <circle cx="11" cy="11" r="8"/>
          <path d="m21 21-4.35-4.35"/>
        </svg>
        <input 
          type="text" 
          id="quickSearch" 
          class="si-input" 
          placeholder="Search or paste link..." 
          autocomplete="off"
          spellcheck="false"
        />
        <div id="siSpinner" class="si-spinner" style="display:none">
          <div class="si-spin-ring"></div>
        </div>
        <button id="siClear" class="si-clear" aria-label="Clear search" style="display:none">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M18 6L6 18M6 6l12 12"/>
          </svg>
        </button>
        <div id="searchDD" class="sdd"></div>
      </div>
      
      <button id="themeToggle" class="header-icon-btn" aria-label="Toggle theme">
        <svg class="theme-sun" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <circle cx="12" cy="12" r="5"/>
          <line x1="12" y1="1" x2="12" y2="3"/>
          <line x1="12" y1="21" x2="12" y2="23"/>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
          <line x1="1" y1="12" x2="3" y2="12"/>
          <line x1="21" y1="12" x2="23" y2="12"/>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
        <svg class="theme-moon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </button>
      
      <button id="profileBtn" class="header-icon-btn" aria-label="Open menu">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
          <circle cx="12" cy="7" r="4"/>
        </svg>
        <span id="profileBadge" class="profile-badge" style="display:none">0</span>
      </button>
      
      <button id="mobileMenuBtn" class="header-icon-btn mobile-only" aria-label="Menu">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
          <line x1="3" y1="6" x2="21" y2="6"/>
          <line x1="3" y1="12" x2="21" y2="12"/>
          <line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
      </button>
    </div>
  </div>
  
  <!-- MOBILE SEARCH BAR — always visible below header on mobile -->
  <div class="mobile-search-bar">
    <div class="msb-inner" id="msbInner">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" style="color:rgba(192,132,252,0.8);flex-shrink:0">
        <circle cx="11" cy="11" r="8"/>
        <path d="m21 21-4.35-4.35"/>
      </svg>
      <input 
        type="text" 
        id="mobileSearch" 
        class="msb-input" 
        placeholder="Search anime or paste link..." 
        autocomplete="off" 
        spellcheck="false" 
      />
      <div id="msbSpinner" class="msb-spinner" style="display:none">
        <div class="si-spin-ring"></div>
      </div>
      <button id="msbClear" class="msb-clear" aria-label="Clear" style="display:none">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M18 6L6 18M6 6l12 12"/>
        </svg>
      </button>
      <div id="msbDD" class="sdd sdd-mobile"></div>
    </div>
  </div>
  
  <div id="mobileMenu" class="mobile-menu">
    <nav class="mobile-nav">
      {navItems.map((item) => (
        <a 
          href={item.href} 
          class={`mobile-nav-link ${currentPath === item.href ? 'active' : ''}`}
        >
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>

<!-- URL Resolve Toast -->
<div id="urlToast" class="url-toast" style="display:none">
  <div class="url-toast-inner">
    <div class="url-toast-spin"></div>
    <div class="url-toast-text">
      <span class="url-toast-title" id="urlToastTitle">Detecting anime...</span>
      <span class="url-toast-sub" id="urlToastSub">Analyzing link with AI</span>
    </div>
    <button class="url-toast-cancel" id="urlToastCancel" aria-label="Cancel">✕</button>
  </div>
</div>

<!-- 🎨 SIDE MENU -->
<div id="sideMenu" class="side-menu">
  <div class="side-menu-backdrop" id="sideMenuBackdrop"></div>
  <aside class="side-menu-panel">
    <div class="sm-header">
      <div class="sm-avatar">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
          <circle cx="12" cy="7" r="4"/>
        </svg>
      </div>
      <div class="sm-user-info">
        <div class="sm-username" id="smUsername">Anime Fan</div>
        <div class="sm-user-sub" id="smUserSub">@guest_user</div>
      </div>
      <button class="sm-close" id="sideMenuClose" aria-label="Close menu">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M18 6L6 18M6 6l12 12"/>
        </svg>
      </button>
    </div>

    <div class="sm-scroll">
      <div class="sm-section">
        <div class="sm-section-label">My Content</div>
        <a href="/saved" class="sm-item">
          <div class="sm-item-icon sm-icon-purple">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
              <path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <div class="sm-item-label">Saved Videos</div>
          <span class="sm-item-count" id="countSaved">0</span>
        </a>
        <a href="/history" class="sm-item">
          <div class="sm-item-icon sm-icon-cyan">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12 6 12 12 16 14"/>
            </svg>
          </div>
          <div class="sm-item-label">Watch History</div>
          <span class="sm-item-count" id="countHistory">0</span>
        </a>
        <a href="/liked" class="sm-item">
          <div class="sm-item-icon sm-icon-pink">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
            </svg>
          </div>
          <div class="sm-item-label">Liked Videos</div>
          <span class="sm-item-count" id="countLiked">0</span>
        </a>
      </div>

      <div class="sm-divider"></div>

      <div class="sm-section">
        <div class="sm-section-label">Community <span class="sm-coming-badge">Soon</span></div>
        <div class="sm-item sm-item-disabled">
          <div class="sm-item-icon sm-icon-grad">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <div class="sm-item-label">Global Chat</div>
          <span class="sm-live-dot"></span>
        </div>
        <div class="sm-item sm-item-disabled">
          <div class="sm-item-icon sm-icon-grad">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <div class="sm-item-label">Friends & Messages</div>
        </div>
      </div>

      <div class="sm-divider"></div>

      <div class="sm-section">
        <div class="sm-section-label">Settings</div>
        <button class="sm-item sm-item-btn" id="smThemeToggle">
          <div class="sm-item-icon sm-icon-purple">
            <svg class="sm-theme-moon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
              <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
            </svg>
            <svg class="sm-theme-sun" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
              <circle cx="12" cy="12" r="5"/>
              <line x1="12" y1="1" x2="12" y2="3"/>
              <line x1="12" y1="21" x2="12" y2="23"/>
            </svg>
          </div>
          <div class="sm-item-label">Switch Appearance</div>
          <span class="sm-item-tag" id="smThemeTag">Dark</span>
        </button>
        <button class="sm-item sm-item-btn" id="smClearData">
          <div class="sm-item-icon sm-icon-red">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="3 6 5 6 21 6"/>
              <path d="M19 6l-2 14a2 2 0 0 1-2 2H9a2 2 0 0 1-2-2L5 6"/>
              <path d="M10 11v6M14 11v6"/>
            </svg>
          </div>
          <div class="sm-item-label">Clear My Data</div>
        </button>
      </div>

      <div class="sm-divider"></div>

      <div class="sm-section">
        <div class="sm-section-label">About</div>
        <a href="/about" class="sm-item">
          <div class="sm-item-icon sm-icon-grad">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <line x1="12" y1="16" x2="12" y2="12"/>
              <line x1="12" y1="8" x2="12.01" y2="8"/>
            </svg>
          </div>
          <div class="sm-item-label">About AniTubeBuzz</div>
        </a>
      </div>

      <div class="sm-bottom-space"></div>
    </div>

    <div class="sm-footer">
      <div class="sm-footer-brand">
        <span class="grad-text">Ani</span><span class="sm-footer-mid">Tube</span><span class="grad-text">Buzz</span>
      </div>
      <div class="sm-footer-tagline">Made with 💜 for anime fans</div>
    </div>
  </aside>
</div>

<script is:inline>
(function() {

  function $(id) { return document.getElementById(id); }

  function isUrl(str) {
    var s = str.trim();
    return s.startsWith('http://') || s.startsWith('https://') ||
           s.indexOf('facebook.com') !== -1 ||
           s.indexOf('fb.watch') !== -1 ||
           s.indexOf('youtu.be') !== -1 ||
           s.indexOf('youtube.com') !== -1 ||
           s.indexOf('dailymotion.com') !== -1 ||
           s.indexOf('bilibili.com') !== -1;
  }

  function esc(s) {
    if (!s) return '';
    return String(s).replace(/[&<>"']/g, function(c) {
      return { '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' }[c];
    });
  }

  function slugify(s) {
    return String(s || '').toLowerCase()
      .replace(/[^a-z0-9]+/g, '-')
      .replace(/^-+|-+$/g, '');
  }

  /* ═══ FUZZY MATCHING ═══ */
  var ANIME_ALIASES = {
    'jjk': 'jujutsu kaisen', 'jjjk': 'jujutsu kaisen',
    'aot': 'attack on titan', 'snk': 'attack on titan',
    'ds': 'demon slayer', 'kny': 'demon slayer',
    'op': 'one piece', 'onep': 'one piece',
    'mha': 'my hero academia', 'bnha': 'my hero academia',
    'sao': 'sword art online',
    'fmab': 'fullmetal alchemist brotherhood', 'fma': 'fullmetal alchemist',
    'hxh': 'hunter x hunter',
    'jjba': 'jojo bizarre adventure', 'jojo': 'jojo bizarre adventure',
    'nge': 'neon genesis evangelion', 'eva': 'neon genesis evangelion',
    'ccs': 'cardcaptor sakura',
    'sds': 'seven deadly sins', 'nnt': 'seven deadly sins',
    'natoro': 'naruto', 'nauto': 'naruto', 'narto': 'naruto',
    'onepice': 'one piece', 'onepeice': 'one piece',
    'csm': 'chainsaw man', 'chainsaw': 'chainsaw man',
    'spy': 'spy x family', 'sxf': 'spy x family',
    'rezero': 're zero',
    'oshi': 'oshi no ko',
    'frieren': 'sousou no frieren',
    'bluelock': 'blue lock',
    'mushoku': 'mushoku tensei',
    'tokrev': 'tokyo revengers',
    'tokyo rev': 'tokyo revengers'
  };

  function normalize(s) {
    return String(s || '').toLowerCase()
      .normalize('NFD')
      .replace(/[\u0300-\u036f]/g, '')
      .replace(/[^a-z0-9\s]/g, ' ')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function expandQuery(q) {
    var norm = normalize(q);
    if (!norm) return [q];
    var queries = [norm];
    if (ANIME_ALIASES[norm]) queries.unshift(ANIME_ALIASES[norm]);
    var words = norm.split(' ');
    var expanded = words.map(function(w) { return ANIME_ALIASES[w] || w; }).join(' ');
    if (expanded !== norm) queries.push(expanded);
    return queries;
  }

  function scoreMatch(title, query) {
    var t = normalize(title);
    var q = normalize(query);
    if (!t || !q) return 0;
    if (t === q) return 100;
    if (t.startsWith(q)) return 90;
    if (t.indexOf(q) !== -1) return 80;
    var qWords = q.split(' ');
    var tWords = t.split(' ');
    var matches = 0;
    for (var i = 0; i < qWords.length; i++) {
      for (var j = 0; j < tWords.length; j++) {
        if (tWords[j].startsWith(qWords[i]) || (qWords[i].length > 3 && tWords[j].indexOf(qWords[i]) !== -1)) {
          matches++; break;
        }
      }
    }
    if (matches > 0) return 40 + (matches / qWords.length) * 30;
    return 0;
  }

  /* THEME */
  var themeBtn = $('themeToggle');
  if (themeBtn) {
    themeBtn.addEventListener('click', function() {
      var current = document.documentElement.getAttribute('data-theme');
      var next = current === 'dark' ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
      updateSmThemeTag();
    });
  }
  function updateSmThemeTag() {
    var tag = $('smThemeTag');
    if (tag) tag.textContent = document.documentElement.getAttribute('data-theme') === 'dark' ? 'Dark' : 'Light';
  }

  /* SCROLL */
  var header = $('siteHeader');
  if (header) {
    window.addEventListener('scroll', function() {
      if (window.scrollY > 20) header.classList.add('scrolled');
      else header.classList.remove('scrolled');
    }, { passive: true });
  }

  /* MOBILE MENU */
  var mobileBtn = $('mobileMenuBtn');
  var mobileMenu = $('mobileMenu');
  if (mobileBtn && mobileMenu) {
    mobileBtn.addEventListener('click', function() {
      mobileMenu.classList.toggle('open');
    });
    mobileMenu.querySelectorAll('a').forEach(function(a) {
      a.addEventListener('click', function() { mobileMenu.classList.remove('open'); });
    });
  }

  /* SIDE MENU */
  var sideMenu = $('sideMenu');
  var sideMenuBackdrop = $('sideMenuBackdrop');
  var sideMenuClose = $('sideMenuClose');

  function openSideMenu() {
    if (!sideMenu) return;
    sideMenu.classList.add('open');
    document.body.style.overflow = 'hidden';
    updateMenuCounts();
    updateSmThemeTag();
    loadUsername();
  }
  function closeSideMenu() {
    if (!sideMenu) return;
    sideMenu.classList.remove('open');
    document.body.style.overflow = '';
  }

  var profileBtn = $('profileBtn');
  if (profileBtn) profileBtn.addEventListener('click', openSideMenu);
  if (sideMenuClose) sideMenuClose.addEventListener('click', closeSideMenu);
  if (sideMenuBackdrop) sideMenuBackdrop.addEventListener('click', closeSideMenu);

  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      if (sideMenu && sideMenu.classList.contains('open')) closeSideMenu();
      closeSearchDD();
      closeMobileSearchDD();
    }
  });

  function loadUsername() {
    try {
      var un = localStorage.getItem('anitube_username');
      var uEl = $('smUsername');
      var subEl = $('smUserSub');
      if (un && uEl) {
        uEl.textContent = un;
        if (subEl) subEl.textContent = '@' + un.toLowerCase().replace(/\s+/g, '_');
      }
    } catch(e) {}
  }

  function updateMenuCounts() {
    try {
      var saved = JSON.parse(localStorage.getItem('anitube_saved') || '[]');
      var hist = JSON.parse(localStorage.getItem('anitube_history') || '[]');
      var liked = JSON.parse(localStorage.getItem('anitube_liked') || '[]');
      var cs = $('countSaved'), ch = $('countHistory'), cl = $('countLiked');
      var badge = $('profileBadge');
      if (cs) cs.textContent = saved.length;
      if (ch) ch.textContent = hist.length;
      if (cl) cl.textContent = liked.length;
      var total = saved.length + hist.length + liked.length;
      if (badge) badge.style.display = total > 0 ? 'flex' : 'none';
    } catch(e) {}
  }
  updateMenuCounts();

  var smThemeToggle = $('smThemeToggle');
  if (smThemeToggle) smThemeToggle.addEventListener('click', function() { if (themeBtn) themeBtn.click(); });

  var smClearData = $('smClearData');
  if (smClearData) {
    smClearData.addEventListener('click', function() {
      if (confirm('Clear all your Saved, History, and Liked videos?')) {
        localStorage.removeItem('anitube_saved');
        localStorage.removeItem('anitube_history');
        localStorage.removeItem('anitube_liked');
        localStorage.removeItem('anitube_resume_video');
        localStorage.removeItem('anitube_resume_time');
        updateMenuCounts();
        alert('Cleared!');
      }
    });
  }

  /* ═══ SEARCH LOGIC ═══ */
  var searchCache = {};

  function searchKitsu(query, ctrl) {
    var url = 'https://kitsu.io/api/edge/anime?filter[text]=' + encodeURIComponent(query) + 
              '&page[limit]=20&fields[anime]=canonicalTitle,titles,posterImage,averageRating,episodeCount,startDate,subtype,slug';
    return fetch(url, { signal: ctrl.signal, headers: { 'Accept': 'application/vnd.api+json' } })
      .then(function(r) { return r.json(); })
      .then(function(data) {
        if (!data || !data.data) return [];
        return data.data.map(function(item) {
          var attr = item.attributes || {};
          var poster = attr.posterImage || {};
          var title = attr.canonicalTitle || (attr.titles ? (attr.titles.en || attr.titles.en_jp || attr.titles.ja_jp) : '') || 'Unknown';
          var year = attr.startDate ? String(attr.startDate).substring(0, 4) : '';
          var rating = attr.averageRating ? (parseFloat(attr.averageRating) / 10).toFixed(1) : '';
          return {
            id: item.id,
            title: title,
            thumb: poster.small || poster.tiny || poster.medium || '',
            year: year,
            episodes: attr.episodeCount || null,
            rating: rating,
            subtype: attr.subtype || '',
            slug: attr.slug || slugify(title)
          };
        });
      });
  }

  function renderResults(results, query, dropdown) {
    if (!dropdown) return;
    if (!results || results.length === 0) {
      dropdown.innerHTML =
        '<div class="hsr-empty">' +
        '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>' +
        '<div><div class="hsr-empty-t">No results for "' + esc(query) + '"</div>' +
        '<div class="hsr-empty-s">Try different spelling</div></div></div>';
      dropdown.classList.add('open');
      return;
    }

    var html = '<div class="hsr-hint">' + results.length + ' results</div>';
    results.forEach(function(item) {
      var thumb = item.thumb || '';
      var title = item.title || 'Unknown';
      var year = item.year || '';
      var eps = item.episodes ? item.episodes + ' EP' : '';
      var rating = item.rating || '';
      var subtype = item.subtype || '';
      var metaParts = [];
      if (subtype) metaParts.push(subtype);
      if (year) metaParts.push(year);
      if (eps) metaParts.push(eps);
      var slug = item.slug || slugify(title);
      var href = '/reels/anime_' + encodeURIComponent(slug) + '_ep1';

      html += '<a href="' + href + '" class="sd-card">';
      html += '<div class="sd-thumb">';
      if (thumb) html += '<img src="' + esc(thumb) + '" alt="" loading="lazy" referrerpolicy="no-referrer" onerror="this.style.display=\'none\'">';
      html += '</div>';
      html += '<div class="sd-body">';
      html += '<div class="sd-title">' + esc(title) + '</div>';
      html += '<div class="sd-meta">';
      if (rating) html += '<span class="sd-rating">★ ' + rating + '</span>';
      metaParts.forEach(function(p, idx) {
        if (idx > 0 || rating) html += '<span class="sd-dot">·</span>';
        html += '<span>' + esc(p) + '</span>';
      });
      html += '</div></div></a>';
    });

    dropdown.innerHTML = html;
    dropdown.classList.add('open');
  }

  function doSearch(rawQuery, dropdown, spinner) {
    var cacheKey = normalize(rawQuery);
    if (searchCache[cacheKey]) {
      renderResults(searchCache[cacheKey], rawQuery, dropdown);
      return;
    }
    if (spinner) spinner.style.display = 'flex';
    var ctrl = new AbortController();
    dropdown.innerHTML = '<div class="hsr-loading"><div class="hsr-spin"></div><span>Searching...</span></div>';
    dropdown.classList.add('open');

    var queries = expandQuery(rawQuery);
    var primaryQuery = queries[0];

    searchKitsu(primaryQuery, ctrl)
      .then(function(results) {
        if (spinner) spinner.style.display = 'none';
        if (results && results.length > 0) {
          results.forEach(function(r) { r._score = scoreMatch(r.title, rawQuery); });
          results.sort(function(a, b) { return b._score - a._score; });
          searchCache[cacheKey] = results;
          renderResults(results, rawQuery, dropdown);
        } else if (queries.length > 1) {
          searchKitsu(queries[1], ctrl).then(function(res2) {
            searchCache[cacheKey] = res2;
            renderResults(res2, rawQuery, dropdown);
          });
        } else {
          renderResults([], rawQuery, dropdown);
        }
      })
      .catch(function(err) {
        if (spinner) spinner.style.display = 'none';
        if (err && err.name === 'AbortError') return;
        dropdown.innerHTML = '<div class="hsr-empty"><div><div class="hsr-empty-t">Search failed</div><div class="hsr-empty-s">Try again</div></div></div>';
        dropdown.classList.add('open');
      });
  }

  /* URL TOAST */
  var urlToast = $('urlToast');
  var urlToastTitle = $('urlToastTitle');
  var urlToastSub = $('urlToastSub');
  var urlToastCancel = $('urlToastCancel');
  var urlResolveCtrl = null;

  function showUrlToast(title, sub) {
    if (!urlToast) return;
    if (urlToastTitle) urlToastTitle.textContent = title || 'Detecting...';
    if (urlToastSub) urlToastSub.textContent = sub || 'Analyzing...';
    urlToast.style.display = 'flex';
    requestAnimationFrame(function() { urlToast.classList.add('visible'); });
  }
  function hideUrlToast() {
    if (!urlToast) return;
    urlToast.classList.remove('visible');
    setTimeout(function() { urlToast.style.display = 'none'; }, 350);
  }
  if (urlToastCancel) {
    urlToastCancel.addEventListener('click', function() {
      if (urlResolveCtrl) urlResolveCtrl.abort();
      hideUrlToast();
    });
  }

  function resolveUrl(url) {
    if (urlResolveCtrl) urlResolveCtrl.abort();
    urlResolveCtrl = new AbortController();
    showUrlToast('Detecting anime...', 'AI analyzing');
    fetch('/api/link-resolver?url=' + encodeURIComponent(url), { signal: urlResolveCtrl.signal })
      .then(function(r) { return r.json(); })
      .then(function(data) {
        if (data && data.success && data.redirectUrl) {
          showUrlToast('Found: ' + (data.title || 'Anime'), 'Redirecting...');
          setTimeout(function() { hideUrlToast(); window.location.href = data.redirectUrl; }, 700);
        } else {
          hideUrlToast();
          alert('Could not detect anime from this link');
        }
      })
      .catch(function(err) {
        if (err && err.name === 'AbortError') return;
        hideUrlToast();
        alert('Failed to resolve link');
      });
  }

  /* DESKTOP SEARCH */
  var searchInput = $('quickSearch');
  var searchDD = $('searchDD');
  var siClear = $('siClear');
  var siSpinner = $('siSpinner');
  var searchTimer = null;

  function closeSearchDD() { if (searchDD) searchDD.classList.remove('open'); }

  if (siClear) {
    siClear.addEventListener('click', function() {
      searchInput.value = ''; siClear.style.display = 'none';
      closeSearchDD(); searchDD.innerHTML = ''; searchInput.focus();
    });
  }

  document.addEventListener('click', function(e) {
    var wrap = $('searchWrap');
    if (wrap && !wrap.contains(e.target)) closeSearchDD();
  });

  if (searchInput) {
    searchInput.addEventListener('input', function() {
      var val = searchInput.value.trim();
      if (siClear) siClear.style.display = val.length > 0 ? 'flex' : 'none';
      if (!val) { closeSearchDD(); searchDD.innerHTML = ''; return; }
      if (isUrl(val)) {
        clearTimeout(searchTimer);
        searchDD.innerHTML =
          '<a href="#" class="sd-linkbtn" id="resolveNowBtn">' +
          '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>' +
          '<span>Find anime from this link</span></a>';
        searchDD.classList.add('open');
        var btn = document.getElementById('resolveNowBtn');
        if (btn) btn.addEventListener('click', function(e) { e.preventDefault(); closeSearchDD(); resolveUrl(val); });
        return;
      }
      clearTimeout(searchTimer);
      searchTimer = setTimeout(function() {
        if (val.length >= 2) doSearch(val, searchDD, siSpinner);
      }, 350);
    });

    searchInput.addEventListener('keydown', function(e) {
      if (e.key === 'Enter') {
        e.preventDefault();
        var val = searchInput.value.trim();
        if (!val) return;
        if (isUrl(val)) { closeSearchDD(); resolveUrl(val); }
        else if (val.length >= 2) doSearch(val, searchDD, siSpinner);
      }
      if (e.key === 'Escape') { closeSearchDD(); searchInput.blur(); }
    });
    searchInput.addEventListener('focus', function() {
      var val = searchInput.value.trim();
      if (val && searchDD.innerHTML.trim()) searchDD.classList.add('open');
    });
  }

  /* MOBILE SEARCH */
  var mobileSearchInput = $('mobileSearch');
  var mobileSearchDD = $('msbDD');
  var msbClear = $('msbClear');
  var msbSpinner = $('msbSpinner');
  var mobileSearchTimer = null;

  function closeMobileSearchDD() { if (mobileSearchDD) mobileSearchDD.classList.remove('open'); }

  if (msbClear) {
    msbClear.addEventListener('click', function() {
      mobileSearchInput.value = ''; msbClear.style.display = 'none';
      closeMobileSearchDD(); mobileSearchDD.innerHTML = ''; mobileSearchInput.focus();
    });
  }

  document.addEventListener('click', function(e) {
    var msb = $('msbInner');
    if (msb && !msb.contains(e.target)) closeMobileSearchDD();
  });

  if (mobileSearchInput) {
    mobileSearchInput.addEventListener('input', function() {
      var val = mobileSearchInput.value.trim();
      if (msbClear) msbClear.style.display = val.length > 0 ? 'flex' : 'none';
      if (!val) { closeMobileSearchDD(); mobileSearchDD.innerHTML = ''; return; }
      if (isUrl(val)) {
        clearTimeout(mobileSearchTimer);
        mobileSearchDD.innerHTML =
          '<a href="#" class="sd-linkbtn" id="mobResolveBtn">' +
          '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/></svg>' +
          '<span>Find anime from this link</span></a>';
        mobileSearchDD.classList.add('open');
        var btn = document.getElementById('mobResolveBtn');
        if (btn) btn.addEventListener('click', function(e) { e.preventDefault(); closeMobileSearchDD(); resolveUrl(val); });
        return;
      }
      clearTimeout(mobileSearchTimer);
      mobileSearchTimer = setTimeout(function() {
        if (val.length >= 2) doSearch(val, mobileSearchDD, msbSpinner);
      }, 350);
    });

    mobileSearchInput.addEventListener('keydown', function(e) {
      if (e.key === 'Enter') {
        e.preventDefault();
        var val = mobileSearchInput.value.trim();
        if (!val) return;
        if (isUrl(val)) { closeMobileSearchDD(); resolveUrl(val); }
        else if (val.length >= 2) doSearch(val, mobileSearchDD, msbSpinner);
      }
    });
  }

})();
</script>

<style is:global>
  /* HEADER */
  .site-header {
    position: sticky; top: 0; left: 0; right: 0;
    z-index: 100;
    background: var(--header-bg);
    border-bottom: 1px solid var(--border-color);
    transition: background 0.25s ease, box-shadow 0.25s ease;
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
  .site-header.scrolled {
    background: var(--header-bg-scrolled);
    box-shadow: 0 4px 20px rgba(0,0,0,0.25);
  }

  .header-inner {
    display: flex; align-items: center; justify-content: space-between;
    height: var(--header-h); gap: 1.5rem;
  }

  .header-logo {
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 1.4rem; font-weight: 800;
    letter-spacing: -0.025em;
    text-decoration: none;
    flex-shrink: 0; line-height: 1;
  }

  .header-nav {
    display: flex; align-items: center; gap: 4px;
    flex: 1; justify-content: center;
  }
  .nav-link {
    display: inline-flex; align-items: center;
    padding: 8px 16px; border-radius: 999px;
    color: var(--text-secondary);
    font-family: 'DM Sans', sans-serif;
    font-size: 0.88rem; font-weight: 600;
    text-decoration: none;
    transition: color 0.15s ease, background 0.15s ease;
  }
  .nav-link:hover { color: var(--text-primary); background: rgba(192,132,252,0.12); }
  .nav-link.active {
    color: var(--nav-link-active-color);
    background: var(--nav-link-active-bg);
    border: 1px solid rgba(192,132,252,0.4);
  }

  .header-right { display: flex; align-items: center; gap: 8px; flex-shrink: 0; }

  /* SEARCH — LARGER 340px */
  .search-inline {
    position: relative;
    display: flex; align-items: center;
    background: rgba(192,132,252,0.08);
    border: 1px solid var(--border-color);
    border-radius: 999px;
    padding: 0 14px;
    transition: border-color 0.2s ease, background 0.2s ease;
    width: 340px;
  }
  [data-theme="light"] .search-inline {
    background: rgba(243,232,255,0.7);
    border-color: rgba(192,132,252,0.3);
  }
  .search-inline:focus-within {
    border-color: rgba(232,121,249,0.7);
    box-shadow: 0 0 0 3px rgba(192,132,252,0.15);
  }

  .si-icon { color: var(--purple); flex-shrink: 0; margin-right: 8px; }
  .si-input {
    background: none; border: none; outline: none;
    color: var(--text-primary);
    font-family: inherit; font-size: 0.85rem;
    padding: 10px 0; width: 100%; min-width: 0;
  }
  .si-input::placeholder { color: var(--text-muted); opacity: 0.7; }

  .si-spinner, .msb-spinner {
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0; margin-left: 6px;
    width: 20px; height: 20px;
  }
  .si-spin-ring {
    width: 14px; height: 14px;
    border: 2px solid rgba(192,132,252,0.3);
    border-top-color: #e879f9;
    border-radius: 50%;
    animation: spin 0.7s linear infinite;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  .si-clear, .msb-clear {
    display: none; align-items: center; justify-content: center;
    background: rgba(192,132,252,0.15);
    border: none; color: var(--text-muted);
    cursor: pointer;
    width: 20px; height: 20px;
    border-radius: 50%;
    flex-shrink: 0; margin-left: 6px; padding: 0;
  }
  .si-clear:hover, .msb-clear:hover { background: rgba(239,68,68,0.3); color: #ef4444; }

  /* DROPDOWN */
  .sdd {
    position: absolute;
    top: calc(100% + 10px);
    right: 0;
    background: rgba(12, 6, 30, 0.98);
    border: 1px solid rgba(192, 132, 252, 0.3);
    border-radius: 14px;
    padding: 8px;
    max-height: 500px;
    overflow-y: auto;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(16px);
    z-index: 500;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-6px);
    transition: opacity 0.2s ease, transform 0.2s ease, visibility 0.2s;
    width: 420px;
  }
  [data-theme="light"] .sdd {
    background: rgba(255, 255, 255, 0.98);
    border-color: rgba(168, 85, 247, 0.25);
  }
  .sdd.open { opacity: 1; visibility: visible; transform: translateY(0); }
  .sdd::-webkit-scrollbar { width: 5px; }
  .sdd::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, rgba(192,132,252,0.4), rgba(232,121,249,0.4));
    border-radius: 6px;
  }
  .sdd-mobile { width: calc(100vw - 24px); right: 12px; left: 12px; }

  .hsr-hint {
    padding: 6px 10px 8px;
    color: #e879f9;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.62rem; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase;
  }

  .hsr-loading {
    display: flex; align-items: center; justify-content: center;
    gap: 10px; padding: 1.8rem 1rem;
    color: rgba(255,255,255,0.6);
    font-family: 'JetBrains Mono', monospace; font-size: 0.72rem;
  }
  [data-theme="light"] .hsr-loading { color: rgba(30,15,54,0.6); }
  .hsr-spin {
    width: 14px; height: 14px;
    border: 2px solid rgba(192,132,252,0.25);
    border-top-color: #e879f9;
    border-radius: 50%;
    animation: spin 0.7s linear infinite;
  }

  .hsr-empty {
    display: flex; align-items: center; gap: 12px;
    padding: 14px;
    color: rgba(255,255,255,0.65);
  }
  [data-theme="light"] .hsr-empty { color: rgba(30,15,54,0.65); }
  .hsr-empty svg { flex-shrink: 0; color: #c084fc; }
  .hsr-empty-t {
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 0.85rem; font-weight: 700;
    color: #ffffff; margin-bottom: 2px;
  }
  [data-theme="light"] .hsr-empty-t { color: #1e0f36; }
  .hsr-empty-s {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.64rem; opacity: 0.75;
  }

  /* CARD */
  .sd-card {
    display: flex !important;
    align-items: center;
    gap: 12px;
    padding: 8px;
    border-radius: 10px;
    text-decoration: none;
    color: inherit;
    transition: background 0.15s ease;
    margin-bottom: 2px;
  }
  .sd-card:hover { background: rgba(192, 132, 252, 0.1); }
  [data-theme="light"] .sd-card:hover { background: rgba(168, 85, 247, 0.08); }

  .sd-thumb {
    width: 56px !important;
    height: 78px !important;
    min-width: 56px !important;
    max-width: 56px !important;
    border-radius: 6px !important;
    overflow: hidden !important;
    background: linear-gradient(135deg, rgba(192,132,252,0.2), rgba(125,211,252,0.1)) !important;
    position: relative !important;
    flex-shrink: 0 !important;
  }
  .sd-thumb img {
    width: 56px !important;
    height: 78px !important;
    max-width: 56px !important;
    max-height: 78px !important;
    min-width: 56px !important;
    min-height: 78px !important;
    object-fit: cover !important;
    display: block !important;
    aspect-ratio: auto !important;
    border-radius: 0 !important;
  }

  .sd-body {
    flex: 1; min-width: 0; overflow: hidden;
    display: flex; flex-direction: column;
    justify-content: center; gap: 5px;
  }
  .sd-title {
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 0.86rem; font-weight: 700;
    color: #ffffff; line-height: 1.3;
    display: -webkit-box; -webkit-line-clamp: 2;
    -webkit-box-orient: vertical; overflow: hidden;
  }
  [data-theme="light"] .sd-title { color: #1e0f36; }
  .sd-meta {
    display: flex; align-items: center; gap: 5px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.65rem;
    color: rgba(255,255,255,0.55);
    text-transform: uppercase; letter-spacing: 0.02em;
    flex-wrap: wrap;
  }
  [data-theme="light"] .sd-meta { color: rgba(30,15,54,0.55); }
  .sd-rating { color: #fbbf24; font-weight: 700; }
  .sd-dot { opacity: 0.4; }

  /* LINK DETECT BUTTON */
  .sd-linkbtn {
    display: flex; align-items: center; gap: 12px;
    padding: 14px;
    border-radius: 10px;
    text-decoration: none;
    color: #ffffff;
    background: linear-gradient(135deg, rgba(232,121,249,0.15), rgba(192,132,252,0.1));
    border: 1px solid rgba(232,121,249,0.3);
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 0.9rem; font-weight: 700;
    transition: background 0.2s;
  }
  .sd-linkbtn:hover { background: linear-gradient(135deg, rgba(232,121,249,0.25), rgba(192,132,252,0.18)); }
  .sd-linkbtn svg { color: #e879f9; flex-shrink: 0; }
  [data-theme="light"] .sd-linkbtn { color: #1e0f36; }

  /* URL TOAST */
  .url-toast {
    position: fixed;
    bottom: 90px; left: 50%;
    transform: translateX(-50%) translateY(20px);
    z-index: 9999;
    opacity: 0;
    transition: opacity 0.3s ease, transform 0.3s ease;
    pointer-events: none;
  }
  .url-toast.visible {
    opacity: 1; transform: translateX(-50%) translateY(0);
    pointer-events: auto;
  }
  .url-toast-inner {
    display: flex; align-items: center; gap: 12px;
    background: rgba(20,9,42,0.95);
    border: 1px solid rgba(192,132,252,0.4);
    border-radius: 14px;
    padding: 12px 16px;
    box-shadow: 0 15px 40px rgba(0,0,0,0.4);
    backdrop-filter: blur(15px);
    min-width: 260px;
  }
  [data-theme="light"] .url-toast-inner {
    background: rgba(255,255,255,0.96);
    border-color: rgba(168,85,247,0.3);
  }
  .url-toast-spin {
    width: 18px; height: 18px; flex-shrink: 0;
    border: 2px solid rgba(192,132,252,0.3);
    border-top-color: #e879f9;
    border-radius: 50%;
    animation: spin 0.7s linear infinite;
  }
  .url-toast-text { flex: 1; min-width: 0; }
  .url-toast-title {
    display: block;
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 0.85rem; font-weight: 700;
    color: #fff; line-height: 1.2;
  }
  [data-theme="light"] .url-toast-title { color: #1e0f36; }
  .url-toast-sub {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.62rem;
    color: rgba(255,255,255,0.6);
    margin-top: 2px;
  }
  [data-theme="light"] .url-toast-sub { color: rgba(30,15,54,0.6); }
  .url-toast-cancel {
    background: rgba(192,132,252,0.15);
    border: none; color: rgba(255,255,255,0.6);
    cursor: pointer;
    width: 24px; height: 24px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  [data-theme="light"] .url-toast-cancel { color: rgba(30,15,54,0.6); }

  /* ICON BTNS */
  .header-icon-btn {
    display: flex; align-items: center; justify-content: center;
    width: 38px; height: 38px;
    border-radius: 50%;
    background: var(--icon-btn-bg);
    border: 1px solid var(--border-color);
    color: var(--icon-btn-color);
    cursor: pointer;
    transition: background 0.15s ease, color 0.15s ease, border-color 0.15s ease;
    flex-shrink: 0; position: relative;
  }
  .header-icon-btn:hover {
    background: rgba(192,132,252,0.2);
    color: var(--text-primary);
    border-color: rgba(192,132,252,0.4);
  }
  .profile-badge {
    position: absolute; top: -2px; right: -2px;
    min-width: 8px; height: 8px;
    border-radius: 50%;
    background: linear-gradient(135deg, #e879f9, #c084fc);
    box-shadow: 0 0 6px rgba(232,121,249,0.6);
  }

  .theme-sun { display: none; }
  .theme-moon { display: block; }
  [data-theme="light"] .theme-sun { display: block; }
  [data-theme="light"] .theme-moon { display: none; }

  /* MOBILE */
  .mobile-only { display: none; }
  
  .mobile-search-bar {
    display: none;
    padding: 8px 12px 10px;
    border-top: 1px solid var(--border-color);
    background: var(--header-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
  
  .msb-inner {
    position: relative;
    display: flex; align-items: center;
    background: rgba(192,132,252,0.1);
    border: 1px solid var(--border-color);
    border-radius: 999px;
    padding: 0 14px;
    transition: border-color 0.2s;
  }
  [data-theme="light"] .msb-inner {
    background: rgba(243,232,255,0.7);
    border-color: rgba(192,132,252,0.3);
  }
  .msb-inner:focus-within {
    border-color: rgba(232,121,249,0.6);
    box-shadow: 0 0 0 2px rgba(192,132,252,0.15);
  }
  
  .msb-input {
    flex: 1;
    background: none; border: none; outline: none;
    color: var(--text-primary);
    font-family: inherit; font-size: 0.9rem;
    padding: 11px 8px;
    min-width: 0;
  }
  .msb-input::placeholder { color: var(--text-muted); opacity: 0.7; }

  .mobile-menu {
    display: none;
    position: absolute; top: 100%; left: 0; right: 0;
    background: var(--header-bg-scrolled);
    border-bottom: 1px solid var(--border-color);
    padding: 12px;
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
  .mobile-menu.open { display: block; }
  .mobile-nav { display: flex; flex-direction: column; gap: 2px; }
  .mobile-nav-link {
    padding: 11px 14px; border-radius: 10px;
    color: var(--text-secondary);
    font-family: 'DM Sans', sans-serif;
    font-size: 0.95rem; font-weight: 600;
    text-decoration: none;
  }
  .mobile-nav-link:hover, .mobile-nav-link.active {
    background: rgba(192,132,252,0.14);
    color: var(--text-primary);
  }

  /* SIDE MENU */
  .side-menu {
    position: fixed; inset: 0;
    z-index: 9999;
    visibility: hidden; pointer-events: none;
    transition: visibility 0.4s;
  }
  .side-menu.open { visibility: visible; pointer-events: auto; }

  .side-menu-backdrop {
    position: absolute; inset: 0;
    background: rgba(10,5,23,0.7);
    backdrop-filter: blur(6px);
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  [data-theme="light"] .side-menu-backdrop { background: rgba(30,15,54,0.5); }
  .side-menu.open .side-menu-backdrop { opacity: 1; }

  .side-menu-panel {
    position: absolute; top: 0; right: 0; bottom: 0;
    width: 340px; max-width: 92vw;
    background: linear-gradient(180deg, #14092a, #0a0517);
    border-left: 1px solid rgba(192,132,252,0.25);
    transform: translateX(100%);
    transition: transform 0.3s ease;
    display: flex; flex-direction: column;
    box-shadow: -20px 0 60px rgba(0,0,0,0.5);
  }
  [data-theme="light"] .side-menu-panel {
    background: linear-gradient(180deg, #ffffff, #faf5ff);
    border-left-color: rgba(168,85,247,0.2);
  }
  .side-menu.open .side-menu-panel { transform: translateX(0); }

  .sm-scroll { flex: 1; overflow-y: auto; }
  .sm-scroll::-webkit-scrollbar { width: 4px; }
  .sm-scroll::-webkit-scrollbar-thumb { background: rgba(192,132,252,0.3); border-radius: 4px; }

  .sm-header {
    display: flex; align-items: center; gap: 12px;
    padding: 20px 18px 18px;
    border-bottom: 1px solid rgba(192,132,252,0.15);
    flex-shrink: 0;
  }
  .sm-avatar {
    width: 44px; height: 44px; border-radius: 50%;
    background: linear-gradient(135deg, #c084fc, #e879f9, #7dd3fc);
    display: flex; align-items: center; justify-content: center;
    color: white; flex-shrink: 0;
  }
  .sm-user-info { flex: 1; min-width: 0; }
  .sm-username {
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 0.95rem; font-weight: 700;
    color: #ffffff;
  }
  [data-theme="light"] .sm-username { color: #1e0f36; }
  .sm-user-sub {
    font-family: 'JetBrains Mono', monospace; font-size: 0.68rem;
    color: rgba(255,255,255,0.5); margin-top: 2px;
  }
  [data-theme="light"] .sm-user-sub { color: rgba(30,15,54,0.55); }
  .sm-close {
    width: 32px; height: 32px; border-radius: 50%;
    background: rgba(192,132,252,0.1);
    border: 1px solid rgba(192,132,252,0.2);
    color: rgba(255,255,255,0.7); cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  [data-theme="light"] .sm-close { color: rgba(30,15,54,0.7); }

  .sm-section { padding: 8px 10px; }
  .sm-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.62rem; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase;
    color: rgba(255,255,255,0.5);
    padding: 6px 10px 8px;
    display: flex; align-items: center; gap: 8px;
  }
  [data-theme="light"] .sm-section-label { color: rgba(30,15,54,0.58); }

  .sm-coming-badge {
    font-family: 'JetBrains Mono', monospace; font-size: 0.55rem;
    padding: 2px 6px;
    background: rgba(232,121,249,0.15);
    color: #e879f9; border-radius: 4px;
    text-transform: none;
  }

  .sm-item {
    display: flex; align-items: center; gap: 12px;
    padding: 9px 10px; border-radius: 10px;
    text-decoration: none;
    background: transparent; border: none;
    width: 100%; cursor: pointer;
    font-family: inherit; text-align: left;
    transition: background 0.15s ease;
    margin-bottom: 2px; color: inherit;
  }
  .sm-item:hover { background: rgba(192,132,252,0.1); }
  [data-theme="light"] .sm-item:hover { background: rgba(168,85,247,0.08); }
  .sm-item-disabled { opacity: 0.5; cursor: not-allowed; }
  .sm-item-disabled:hover { background: transparent; }

  .sm-item-icon {
    width: 34px; height: 34px; border-radius: 9px;
    display: flex; align-items: center; justify-content: center;
    color: white; flex-shrink: 0;
  }
  .sm-item-icon svg { width: 16px; height: 16px; }
  .sm-icon-purple { background: linear-gradient(135deg, #a855f7, #c084fc); }
  .sm-icon-pink   { background: linear-gradient(135deg, #ec4899, #e879f9); }
  .sm-icon-cyan   { background: linear-gradient(135deg, #06b6d4, #7dd3fc); }
  .sm-icon-red    { background: linear-gradient(135deg, #ef4444, #f87171); }
  .sm-icon-grad   { background: linear-gradient(135deg, #c084fc, #e879f9, #7dd3fc); }

  .sm-item-label {
    flex: 1; font-family: 'DM Sans', sans-serif;
    font-size: 0.88rem; font-weight: 600;
    color: #ffffff;
  }
  [data-theme="light"] .sm-item-label { color: #1e0f36; }

  .sm-item-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.68rem; font-weight: 700;
    color: #e879f9;
    background: rgba(232,121,249,0.12);
    padding: 3px 8px; border-radius: 8px;
    min-width: 24px; text-align: center;
  }
  [data-theme="light"] .sm-item-count { color: #a855f7; background: rgba(168,85,247,0.1); }

  .sm-item-tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.62rem; font-weight: 700;
    color: rgba(255,255,255,0.7);
    background: rgba(192,132,252,0.08);
    padding: 3px 8px; border-radius: 8px;
    border: 1px solid rgba(192,132,252,0.2);
  }
  [data-theme="light"] .sm-item-tag { color: rgba(30,15,54,0.7); }

  .sm-live-dot {
    width: 7px; height: 7px; border-radius: 50%;
    background: #10b981;
  }

  .sm-theme-sun { display: none; }
  .sm-theme-moon { display: block; }
  [data-theme="light"] .sm-theme-sun { display: block; }
  [data-theme="light"] .sm-theme-moon { display: none; }

  .sm-divider {
    height: 1px; margin: 4px 18px;
    background: linear-gradient(90deg, transparent, rgba(192,132,252,0.2), transparent);
  }

  .sm-bottom-space { height: 100px; }

  .sm-footer {
    padding: 12px 18px 18px;
    border-top: 1px solid rgba(192,132,252,0.12);
    text-align: center; flex-shrink: 0;
    margin-bottom: 70px;
  }
  .sm-footer-brand {
    font-family: 'Bricolage Grotesque', sans-serif;
    font-size: 1rem; font-weight: 800;
    margin-bottom: 4px;
  }
  .sm-footer-mid { color: #ffffff; }
  [data-theme="light"] .sm-footer-mid { color: #1e0f36; }
  .sm-footer-tagline {
    font-family: 'JetBrains Mono', monospace; font-size: 0.65rem;
    color: rgba(255,255,255,0.48);
  }
  [data-theme="light"] .sm-footer-tagline { color: rgba(30,15,54,0.52); }

  /* RESPONSIVE */
  @media (max-width: 1100px) {
    .search-inline { width: 280px; }
    .sdd { width: 380px; }
    .nav-link { padding: 7px 12px; font-size: 0.85rem; }
  }
  @media (max-width: 900px) {
    .search-inline { width: 220px; }
    .sdd { width: 340px; }
  }
  @media (max-width: 768px) {
    .header-nav { display: none; }
    .search-inline { display: none; }
    .mobile-only { display: flex; }
    .mobile-search-bar { display: block; }
    .header-right { gap: 6px; }
    .header-icon-btn { width: 34px; height: 34px; }
    .side-menu-panel { width: 100%; max-width: 100%; }
    .sm-footer { margin-bottom: 100px; }
    .sm-bottom-space { height: 120px; }
    .url-toast { bottom: 80px; }
  }
</style>
