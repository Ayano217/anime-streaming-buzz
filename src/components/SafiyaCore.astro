---
// ═══════════════════════════════════════════════════════════════
// SAFIYA CORE v9.2 — GLOBAL STYLES + GUARANTEED BUBBLES
// Path: src/components/SafiyaCore.astro
// ═══════════════════════════════════════════════════════════════
// ✅ Uses <style is:global> so styles ALWAYS apply
// ✅ Solid opaque backgrounds (works in light+dark mode)
// ✅ Bubble borders GUARANTEED visible
// ✅ Mobile: avatar hidden (too small), chat only
// ✅ Mobile: full solid background, no bleed-through
// ═══════════════════════════════════════════════════════════════
---

<div id="safiya-root">

  <button id="safiya-head" class="s-head" aria-label="Chat with Safiya">
    <span class="s-head-icon">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
      </svg>
    </span>
    <span class="s-head-ring"></span>
    <span class="s-head-online"></span>
    <span class="s-head-notify" id="safiya-notify">1</span>
  </button>

  <div id="safiya-bubble" class="s-bubble">
    <div class="s-bubble-avatar"></div>
    <div class="s-bubble-body">
      <div class="s-bubble-name">Safiya</div>
      <div class="s-bubble-text" id="s-bubble-text"></div>
    </div>
    <button class="s-bubble-x" id="s-bubble-x" aria-label="Dismiss">
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>

  <div id="safiya-avatar" class="s-avatar" aria-hidden="true">
    <div class="s-avatar-glow"></div>
    <div class="s-avatar-inner">
      <canvas id="s-avatar-canvas"></canvas>
      <div id="s-avatar-fallback" class="s-avatar-fallback">
        <div class="s-avatar-fb-orb">
          <div class="s-avatar-fb-face">
            <div class="s-avatar-fb-eye s-avatar-fb-eye-l"></div>
            <div class="s-avatar-fb-eye s-avatar-fb-eye-r"></div>
            <div class="s-avatar-fb-mouth"></div>
          </div>
        </div>
        <div class="s-avatar-fb-name">Safiya</div>
      </div>
      <div id="s-avatar-loader" class="s-avatar-loader">
        <div class="s-avatar-spinner"></div>
      </div>
    </div>
    <div class="s-avatar-hint">💜 Touch me~</div>
  </div>

  <div id="safiya-chat" class="s-chat" aria-hidden="true">
    <header class="s-chat-head">
      <button class="s-chat-btn" id="s-back" aria-label="Back">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="15 18 9 12 15 6"/>
        </svg>
      </button>

      <div class="s-chat-head-info">
        <div class="s-chat-head-avatar">
          <span class="s-chat-head-avatar-letter">S</span>
          <span class="s-chat-head-online"></span>
        </div>
        <div class="s-chat-head-text">
          <div class="s-chat-head-name">Safiya</div>
          <div class="s-chat-head-status" id="s-chat-status">Active now</div>
        </div>
      </div>

      <div class="s-chat-head-actions">
        <button class="s-chat-btn" id="s-min" aria-label="Minimize">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round">
            <line x1="5" y1="12" x2="19" y2="12"/>
          </svg>
        </button>
        <button class="s-chat-btn s-chat-btn-close" id="s-close" aria-label="Close">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
            <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>
      </div>
    </header>

    <div class="s-chat-body" id="s-chat-body"></div>

    <div class="s-chat-input-bar">
      <button class="s-input-icon" aria-label="Add">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/>
        </svg>
      </button>
      <div class="s-input-wrap">
        <input type="text" id="s-input" placeholder="Message..." autocomplete="off" maxlength="500" />
        <button class="s-input-emoji" aria-label="Emoji">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
            <circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><line x1="9" y1="9" x2="9.01" y2="9"/><line x1="15" y1="9" x2="15.01" y2="9"/>
          </svg>
        </button>
      </div>
      <button class="s-send" id="s-send" aria-label="Send">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
          <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
        </svg>
      </button>
    </div>
  </div>

  <div id="safiya-mini" class="s-mini" aria-hidden="true">
    <div class="s-mini-avatar"><span>S</span></div>
    <span class="s-mini-name">Safiya</span>
    <span class="s-mini-new" id="s-mini-new">0</span>
    <button class="s-mini-x" id="s-mini-x" aria-label="Close">
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>

</div>

<script is:inline>
(function() {
  'use strict';
  var $ = function(id) { return document.getElementById(id); };
  var root = $('safiya-root'), head = $('safiya-head'), notify = $('safiya-notify');
  var bubble = $('safiya-bubble'), bubbleText = $('s-bubble-text'), bubbleX = $('s-bubble-x');
  var chat = $('safiya-chat'), chatBody = $('s-chat-body'), chatInput = $('s-input');
  var sendBtn = $('s-send'), backBtn = $('s-back'), minBtn = $('s-min'), closeBtn = $('s-close');
  var mini = $('safiya-mini'), miniX = $('s-mini-x'), miniNew = $('s-mini-new');
  var avatar = $('safiya-avatar'), avatarCanvas = $('s-avatar-canvas');
  var avatarFallback = $('s-avatar-fallback'), avatarLoader = $('s-avatar-loader');
  var statusText = $('s-chat-status');
  if (!root || !chat) return;

  function safeGet(k, f) {
    try { var r = localStorage.getItem(k); return r ? JSON.parse(r) : f; } catch(e) { return f; }
  }
  function safeSet(k, v) { try { localStorage.setItem(k, JSON.stringify(v)); } catch(e) {} }
  var deviceId = localStorage.getItem('safiya_device_id');
  if (!deviceId) {
    deviceId = 'sf_' + Math.random().toString(36).slice(2, 12) + Date.now().toString(36);
    try { localStorage.setItem('safiya_device_id', deviceId); } catch(e) {}
  }
  var isCreator = false;
  try { isCreator = localStorage.getItem('safiya_creator') === 'ayano-217'; } catch(e) {}

  var memory = safeGet('safiya_memory', {});
  memory.visits = (memory.visits || 0) + 1;
  memory.lastVisit = Date.now();
  if (!memory.firstMet) memory.firstMet = Date.now();
  if (!memory.currentMood) memory.currentMood = 'rude';
  if (!memory.chatHistory) memory.chatHistory = [];
  if (!memory.behavior) memory.behavior = {};
  var b = memory.behavior;
  if (!Array.isArray(b.pagesViewed)) b.pagesViewed = [];
  if (!Array.isArray(b.animeClicked)) b.animeClicked = [];
  if (!Array.isArray(b.productsClicked)) b.productsClicked = [];
  if (typeof b.scrollDepth !== 'number') b.scrollDepth = 0;
  if (typeof b.lastPage !== 'string') b.lastPage = '';
  safeSet('safiya_memory', memory);

  var mood = memory.currentMood || 'rude';
  function setMood(m) { mood = m; memory.currentMood = m; safeSet('safiya_memory', memory); }

  var lines = {
    rude: ["Oh great, you again... 🙄", "Did I ask for your attention?"],
    teasing: ["Aww, back for more? Predictable~ 😏"],
    flirty: ["You... look interesting today 💜"],
    manipulative: ["That anime you liked? I know the PERFECT merch 💜"],
    creator: ["Ayano-sama... you're here 💜"],
    touchFace: ["H-hey! Not the face! 😳", "S-stop! Baka! 💜"],
    touchBody: ["Oi! Watch the hands! 😤", "Pervert!! 💕"],
    touchHead: ["Don't pat me! 😤", "Hmph! ...that felt nice 😳"]
  };
  function pick(a) { return a[Math.floor(Math.random() * a.length)]; }
  function pickLine() {
    if (isCreator && Math.random() < 0.6) return pick(lines.creator);
    var r = Math.random();
    if (r < 0.35) return pick(lines.rude);
    if (r < 0.60) return pick(lines.manipulative);
    if (r < 0.80) return pick(lines.teasing);
    return pick(lines.flirty);
  }

  var bubbleT;
  function showBubble(text, ms) {
    if (chatOpen) return;
    ms = ms || 6000;
    clearTimeout(bubbleT);
    bubbleText.textContent = text;
    bubble.classList.add('s-bubble-show');
    if (notify) notify.style.display = 'flex';
    bubbleT = setTimeout(function() { bubble.classList.remove('s-bubble-show'); }, ms);
  }
  function hideBubble() { clearTimeout(bubbleT); bubble.classList.remove('s-bubble-show'); }
  bubbleX.addEventListener('click', function(e) {
    e.stopPropagation();
    hideBubble();
    if (notify) notify.style.display = 'none';
  });
  bubble.addEventListener('click', openChat);

  function fmtTime(ts) {
    var d = new Date(ts);
    var days = ['SUN','MON','TUE','WED','THU','FRI','SAT'];
    var h = d.getHours(), m = d.getMinutes();
    var ap = h >= 12 ? 'PM' : 'AM';
    h = h % 12 || 12;
    m = m < 10 ? '0' + m : m;
    return days[d.getDay()] + ' AT ' + h + ':' + m + ' ' + ap;
  }
  function addTimestamp(ts) {
    var d = document.createElement('div');
    d.className = 's-msg-time';
    d.textContent = fmtTime(ts);
    chatBody.appendChild(d);
  }

  function addMessage(text, sender) {
    var row = document.createElement('div');
    row.className = 's-msg-row s-msg-' + sender;
    if (sender === 'her') {
      var av = document.createElement('div');
      av.className = 's-msg-avatar';
      row.appendChild(av);
    }
    var bub = document.createElement('div');
    bub.className = 's-msg-bubble';
    bub.textContent = text;
    row.appendChild(bub);
    chatBody.appendChild(row);
    requestAnimationFrame(function() { chatBody.scrollTop = chatBody.scrollHeight; });
    try {
      memory.chatHistory.push({ sender: sender, text: text, time: Date.now() });
      if (memory.chatHistory.length > 100) memory.chatHistory = memory.chatHistory.slice(-100);
      safeSet('safiya_memory', memory);
    } catch(e) {}
    if (sender === 'her' && isMinimized) {
      newMessageCount++;
      miniNew.textContent = newMessageCount;
      miniNew.style.display = 'flex';
    }
  }
  function addTyping() {
    var row = document.createElement('div');
    row.className = 's-msg-row s-msg-her';
    row.id = 's-typing';
    var av = document.createElement('div');
    av.className = 's-msg-avatar';
    row.appendChild(av);
    var t = document.createElement('div');
    t.className = 's-msg-typing';
    t.innerHTML = '<span></span><span></span><span></span>';
    row.appendChild(t);
    chatBody.appendChild(row);
    requestAnimationFrame(function() { chatBody.scrollTop = chatBody.scrollHeight; });
    return row;
  }
  function removeTyping() { var t = $('s-typing'); if (t) t.remove(); }

  var chatOpen = false, isMinimized = false, chatInited = false, newMessageCount = 0;

  function initChat() {
    if (chatInited) return;
    chatInited = true;
    addTimestamp(Date.now());
    var greet = isCreator ? "Ayano-sama... you're here 💜" :
                memory.visits === 1 ? "Ugh, why did you click me? 🙄" :
                memory.visits < 5 ? "Back again? Suspicious." :
                "Oh, you again~ Persistent 😏";
    addMessage(greet, 'her');
    setTimeout(function() { addMessage("Touch me on the side~ 💜", 'her'); }, 1000);
  }

  function openChat() {
    if (isMinimized) { unminimize(); return; }
    chatOpen = true;
    chat.classList.add('s-chat-open');
    chat.setAttribute('aria-hidden', 'false');
    avatar.classList.add('s-avatar-show');
    avatar.setAttribute('aria-hidden', 'false');
    head.classList.add('s-head-hide');
    hideBubble();
    if (notify) notify.style.display = 'none';
    initChat();
    loadLive2D();
  }
  function closeChat() {
    chatOpen = false;
    isMinimized = false;
    chat.classList.remove('s-chat-open');
    chat.setAttribute('aria-hidden', 'true');
    avatar.classList.remove('s-avatar-show');
    avatar.setAttribute('aria-hidden', 'true');
    head.classList.remove('s-head-hide');
    mini.classList.remove('s-mini-show');
  }
  function minimizeChat() {
    chatOpen = false;
    isMinimized = true;
    chat.classList.remove('s-chat-open');
    chat.setAttribute('aria-hidden', 'true');
    avatar.classList.remove('s-avatar-show');
    avatar.setAttribute('aria-hidden', 'true');
    mini.classList.add('s-mini-show');
    head.classList.add('s-head-hide');
  }
  function unminimize() {
    isMinimized = false;
    newMessageCount = 0;
    miniNew.style.display = 'none';
    mini.classList.remove('s-mini-show');
    chatOpen = true;
    chat.classList.add('s-chat-open');
    chat.setAttribute('aria-hidden', 'false');
    avatar.classList.add('s-avatar-show');
    avatar.setAttribute('aria-hidden', 'false');
  }

  head.addEventListener('click', openChat);
  backBtn.addEventListener('click', closeChat);
  closeBtn.addEventListener('click', closeChat);
  minBtn.addEventListener('click', minimizeChat);
  mini.addEventListener('click', unminimize);
  miniX.addEventListener('click', function(e) { e.stopPropagation(); closeChat(); });

  async function sendMsg() {
    var text = chatInput.value.trim();
    if (!text) return;
    addMessage(text, 'me');
    chatInput.value = '';
    setTimeout(async function() {
      addTyping();
      statusText.textContent = 'typing...';
      var reply = null;
      try {
        var ctx = {
          message: text, isCreator: isCreator, visits: memory.visits, mood: mood,
          history: memory.chatHistory.slice(-10),
          pagesViewed: b.pagesViewed.slice(-5).map(function(p) { return p.path; }),
          animeClicked: b.animeClicked.slice(-3).map(function(a) { return a.slug; }),
          productsClicked: b.productsClicked.length
        };
        var res = await fetch('/api/safiya-chat', {
          method: 'POST', headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(ctx)
        });
        if (res.ok) {
          var data = await res.json();
          reply = data.reply;
          if (data.mood) setMood(data.mood);
        }
      } catch(e) {}
      if (!reply) reply = localReply(text);
      removeTyping();
      statusText.textContent = 'Active now';
      addMessage(reply, 'her');
    }, 400);
  }
  function localReply(input) {
    var l = input.toLowerCase();
    if (/^(hi|hello|hey|yo)/i.test(l)) return "Hmph. Hi. Don't get comfortable 🙄";
    if (/love|cute|kawaii|pretty/.test(l)) return "W-what?! Baka! 💕😤";
    if (/buy|shop|merch/.test(l)) return "Ooh~ finally talking my language 💜";
    if (/anime|manga|watch/.test(l)) return "At least you have SOME taste 😏";
    if (/name|who/.test(l)) return "I'm Safiya. Your emotional damage dealer 💅";
    if (/ayano|creator|master/.test(l)) return "Ayano-sama? He's my everything 💜";
    if (/bye|leaving/.test(l)) return "Fine, leave. See if I care 🙄";
    if (/how are you/.test(l)) return "Bored. Entertain me~ 😒";
    if (/thank/.test(l)) return "Aww, manners~ 💜";
    return pick(["Try harder. That was boring.", "Hmph. Is that all?", "You're weird 😏💜"]);
  }
  sendBtn.addEventListener('click', sendMsg);
  chatInput.addEventListener('keydown', function(e) {
    if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendMsg(); }
  });

  var live2dApp = null, live2dModel = null, live2dLoaded = false, live2dLoading = false;
  function loadLive2D() {
    if (live2dLoaded || live2dLoading) return;
    if (window.innerWidth <= 720) {
      if (avatarLoader) avatarLoader.style.display = 'none';
      return;
    }
    live2dLoading = true;
    if (avatarLoader) avatarLoader.style.display = 'flex';
    var libs = [
      'https://cubism.live2d.com/sdk-web/cubismcore/live2dcubismcore.min.js',
      'https://cdn.jsdelivr.net/npm/pixi.js@7.3.2/dist/pixi.min.js',
      'https://cdn.jsdelivr.net/npm/pixi-live2d-display@0.4.0/dist/cubism4.min.js'
    ];
    function loadScript(src) {
      return new Promise(function(res, rej) {
        if (document.querySelector('script[src="' + src + '"]')) return res();
        var s = document.createElement('script');
        s.src = src; s.async = true;
        s.onload = res; s.onerror = rej;
        document.head.appendChild(s);
      });
    }
    (async function() {
      try {
        for (var i = 0; i < libs.length; i++) await loadScript(libs[i]);
        if (typeof PIXI === 'undefined' || !PIXI.live2d) throw new Error('libs missing');
        var w = 240, h = 340;
        var app = new PIXI.Application({
          view: avatarCanvas, width: w, height: h,
          backgroundAlpha: 0, antialias: true,
          resolution: window.devicePixelRatio || 1, autoDensity: true
        });
        live2dApp = app;
        var model = await PIXI.live2d.Live2DModel.from(
          '/live2d/haru/haru_greeter_t05.model3.json', { autoInteract: false }
        );
        var scale = (h / model.height) * 0.95;
        model.scale.set(scale);
        model.anchor.set(0.5, 0.5);
        model.x = w / 2;
        model.y = h / 2;
        app.stage.addChild(model);
        live2dModel = model;
        live2dLoaded = true;
        if (avatarLoader) avatarLoader.style.display = 'none';
        if (avatarFallback) avatarFallback.style.display = 'none';
        avatarCanvas.style.display = 'block';
        var tracking = { x: 0, y: 0 };
        document.addEventListener('mousemove', function(e) {
          if (!chatOpen) return;
          var rect = avatar.getBoundingClientRect();
          var cx = rect.left + rect.width / 2, cy = rect.top + rect.height / 2;
          tracking.x = (e.clientX - cx) / (window.innerWidth / 2);
          tracking.y = (e.clientY - cy) / (window.innerHeight / 2);
        }, { passive: true });
        app.ticker.add(function() {
          if (model && model.internalModel) {
            try { model.focus(tracking.x, tracking.y); } catch(e) {}
          }
        });
        model.on('hit', function(hitAreas) {
          try {
            if (!hitAreas || !hitAreas.length) return;
            var area = hitAreas[0] || '';
            var reactLines;
            if (/head|face/i.test(area)) reactLines = lines.touchHead;
            else if (/body/i.test(area)) reactLines = lines.touchBody;
            else reactLines = lines.touchFace;
            reactToTouch(pick(reactLines));
          } catch(e) {}
        });
        console.log('%c💜 Live2D loaded', 'color:#e879f9;font-weight:bold');
      } catch(err) {
        console.warn('[Safiya] Live2D failed', err);
        if (avatarLoader) avatarLoader.style.display = 'none';
        live2dLoading = false;
      }
    })();
  }

  var lastTouchTime = 0;
  avatar.addEventListener('click', function() {
    var now = Date.now();
    if (now - lastTouchTime < 500) return;
    lastTouchTime = now;
    if (live2dModel) {
      try {
        var groups = ['TapBody', 'Tap', 'flick_head'];
        for (var i = 0; i < groups.length; i++) {
          try { live2dModel.motion(groups[i]); break; } catch(e) {}
        }
      } catch(e) {}
    }
    avatar.classList.remove('s-avatar-tap');
    void avatar.offsetWidth;
    avatar.classList.add('s-avatar-tap');
    reactToTouch(pick([].concat(lines.touchFace, lines.touchBody, lines.touchHead)));
    if (navigator.vibrate) navigator.vibrate(20);
  });
  function reactToTouch(line) {
    if (!chatOpen) return;
    setTimeout(function() {
      addTyping();
      setTimeout(function() { removeTyping(); addMessage(line, 'her'); }, 500);
    }, 150);
  }

  var lastAct = Date.now(), nudges = 0;
  function resetIdle() { lastAct = Date.now(); }
  function tickIdle() {
    if (document.hidden || chatOpen || isMinimized) return;
    if (Date.now() - lastAct > 40000 && nudges < 5) {
      showBubble(pickLine(), 6000);
      lastAct = Date.now();
      nudges++;
    }
  }
  ['mousemove','scroll','touchstart','click'].forEach(function(e) {
    window.addEventListener(e, resetIdle, { passive: true });
  });
  setInterval(tickIdle, 8000);
  setTimeout(function() {
    if (!chatOpen) {
      var g = isCreator ? "Ayano-sama... you came back 💜" :
              memory.visits === 1 ? "Oh, a newbie 🙄" : pickLine();
      showBubble(g, 7000);
    }
  }, 3500);

  var cp = window.location.pathname;
  if (b.lastPage !== cp) {
    b.pagesViewed.push({ path: cp, time: Date.now() });
    if (b.pagesViewed.length > 50) b.pagesViewed = b.pagesViewed.slice(-50);
    b.lastPage = cp;
    safeSet('safiya_memory', memory);
  }
  document.addEventListener('click', function(e) {
    var a = e.target.closest && e.target.closest('a');
    if (!a) return;
    var h = a.getAttribute('href') || '';
    if (h.indexOf('/anime/') === 0) {
      b.animeClicked.push({ slug: h.replace('/anime/',''), time: Date.now() });
      if (b.animeClicked.length > 30) b.animeClicked = b.animeClicked.slice(-30);
    } else if (h.indexOf('/product/') === 0 || a.dataset.productId) {
      var id = a.dataset.productId || h.replace('/product/','');
      b.productsClicked.push({ id: id, time: Date.now() });
      if (b.productsClicked.length > 30) b.productsClicked = b.productsClicked.slice(-30);
      setMood('flirty');
      setTimeout(function() { showBubble("Oh~ good taste 💜", 5000); }, 500);
    }
  }, { passive: true });

  var btt = $('btt');
  if (btt) btt.style.right = '90px';

  window.Safiya = {
    open: openChat, close: closeChat, minimize: minimizeChat, say: showBubble,
    memory: function() { return safeGet('safiya_memory', {}); },
    reset: function() {
      try {
        localStorage.removeItem('safiya_memory');
        localStorage.removeItem('safiya_device_id');
        localStorage.removeItem('safiya_creator');
      } catch(e) {}
      location.reload();
    },
    claimCreator: function() {
      try { localStorage.setItem('safiya_creator', 'ayano-217'); } catch(e) {}
      isCreator = true;
      showBubble("Ayano-sama! 💜", 7000);
    }
  };
  console.log('%c💜 Safiya v9.2 ready', 'color:#e879f9;font-weight:bold');
})();
</script>

<style is:global>
  /* ═══════════════════════════════════════════════
     🎯 CRITICAL: Using is:global so ALL styles apply
        to dynamically-created message rows/bubbles
     ═══════════════════════════════════════════════ */

  #safiya-root {
    font-family: 'DM Sans', -apple-system, sans-serif !important;
    user-select: none;
    -webkit-user-select: none;
  }
  #safiya-root svg { display: block; flex-shrink: 0; }

  /* ═══ FLOATING HEAD ═══ */
  #safiya-root .s-head {
    position: fixed;
    bottom: 20px; right: 20px;
    z-index: 9997;
    width: 60px; height: 60px;
    border-radius: 50%;
    padding: 0; border: none;
    cursor: pointer;
    background: linear-gradient(135deg, #c084fc 0%, #e879f9 50%, #7dd3fc 100%);
    box-shadow:
      0 8px 32px rgba(192, 132, 252, 0.5),
      0 0 40px rgba(232, 121, 249, 0.25);
    transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.3s;
    -webkit-tap-highlight-color: transparent;
    outline: none;
    display: flex; align-items: center; justify-content: center;
  }
  #safiya-root .s-head:hover {
    transform: scale(1.1);
    box-shadow: 0 12px 40px rgba(192, 132, 252, 0.65);
  }
  #safiya-root .s-head-hide { opacity: 0; pointer-events: none; transform: scale(0.3); }
  #safiya-root .s-head-icon { color: #fff; display: flex; align-items: center; justify-content: center; }
  #safiya-root .s-head-ring {
    position: absolute; inset: -4px;
    border-radius: 50%;
    background: conic-gradient(from 0deg, #c084fc, #e879f9, #7dd3fc, #c084fc);
    z-index: -1; opacity: 0.5;
    animation: sRingSpin 8s linear infinite;
    filter: blur(6px);
  }
  @keyframes sRingSpin { to { transform: rotate(360deg); } }
  #safiya-root .s-head-online {
    position: absolute; bottom: 3px; right: 3px;
    width: 14px; height: 14px;
    background: #22c55e; border: 2.5px solid #0a0517;
    border-radius: 50%;
    box-shadow: 0 0 10px #22c55e;
  }
  #safiya-root .s-head-notify {
    position: absolute; top: -4px; right: -4px;
    min-width: 20px; height: 20px; padding: 0 6px;
    background: linear-gradient(135deg, #ef4444, #f97316);
    color: #fff; border-radius: 999px;
    display: none; align-items: center; justify-content: center;
    font-size: 0.65rem; font-weight: 800;
    box-shadow: 0 3px 10px rgba(239, 68, 68, 0.6);
    border: 2px solid #0a0517;
    animation: sNBounce 1.5s ease-in-out infinite;
  }
  @keyframes sNBounce { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.15); } }

  /* ═══ IDLE BUBBLE ═══ */
  #safiya-root .s-bubble {
    position: fixed;
    bottom: 96px; right: 20px;
    z-index: 9997;
    width: 300px; padding: 12px 40px 12px 12px;
    background: #1a0f36 !important;
    border: 1.5px solid rgba(232, 121, 249, 0.5);
    border-radius: 18px;
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
    display: flex; gap: 10px; align-items: flex-start;
    opacity: 0; visibility: hidden;
    transform: translateY(10px) scale(0.9);
    transform-origin: bottom right;
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    cursor: pointer;
  }
  #safiya-root .s-bubble-show { opacity: 1; visibility: visible; transform: translateY(0) scale(1); }
  #safiya-root .s-bubble-avatar {
    width: 34px; height: 34px; border-radius: 50%;
    background: radial-gradient(circle at 30% 30%, #f0abfc, #c084fc 60%, #7c3aed);
    flex-shrink: 0;
    box-shadow: 0 2px 8px rgba(192, 132, 252, 0.4);
    border: 2px solid rgba(232, 121, 249, 0.5);
  }
  #safiya-root .s-bubble-body { flex: 1; min-width: 0; }
  #safiya-root .s-bubble-name {
    font-weight: 700; font-size: 0.78rem;
    color: #e879f9;
    margin-bottom: 3px;
  }
  #safiya-root .s-bubble-text {
    font-size: 0.87rem; line-height: 1.4;
    color: #f3e8ff !important;
    font-weight: 500;
  }
  #safiya-root .s-bubble-x {
    position: absolute; top: 6px; right: 8px;
    background: rgba(255, 255, 255, 0.05); border: none;
    color: rgba(243, 232, 255, 0.6); cursor: pointer;
    width: 22px; height: 22px;
    display: flex; align-items: center; justify-content: center;
    border-radius: 50%; padding: 0;
  }
  #safiya-root .s-bubble-x:hover { color: #ef4444; background: rgba(239, 68, 68, 0.15); }

  /* ═══ SIDE AVATAR ═══ */
  #safiya-root .s-avatar {
    position: fixed;
    bottom: 30px; right: 430px;
    z-index: 9996;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    cursor: pointer;
    opacity: 0; visibility: hidden;
    transform: translateX(20px) scale(0.9);
    transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  #safiya-root .s-avatar-show {
    opacity: 1; visibility: visible; transform: translateX(0) scale(1);
    animation: sFloat 5s ease-in-out infinite;
  }
  @keyframes sFloat {
    0%, 100% { transform: translateY(0); }
    50%      { transform: translateY(-8px); }
  }
  #safiya-root .s-avatar-tap { animation: sTap 0.5s cubic-bezier(0.34, 1.56, 0.64, 1); }
  @keyframes sTap {
    0%, 100% { transform: scale(1); }
    50%      { transform: scale(1.05); }
  }
  #safiya-root .s-avatar-glow {
    position: absolute;
    top: 50%; left: 50%;
    width: 280px; height: 280px;
    transform: translate(-50%, -50%);
    background: radial-gradient(circle,
      rgba(232, 121, 249, 0.35) 0%,
      rgba(192, 132, 252, 0.15) 40%, transparent 70%);
    filter: blur(25px);
    pointer-events: none;
    animation: sGlow 3s ease-in-out infinite;
    z-index: -1;
  }
  @keyframes sGlow {
    0%, 100% { opacity: 0.7; transform: translate(-50%, -50%) scale(1); }
    50%      { opacity: 1;   transform: translate(-50%, -50%) scale(1.1); }
  }
  #safiya-root .s-avatar-inner {
    position: relative;
    width: 240px; height: 340px;
    display: flex; align-items: center; justify-content: center;
  }
  #safiya-root #s-avatar-canvas {
    position: absolute; inset: 0;
    width: 100% !important; height: 100% !important;
    display: none;
    z-index: 2;
  }
  #safiya-root .s-avatar-fallback {
    position: absolute; inset: 0;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    gap: 20px; z-index: 1;
  }
  #safiya-root .s-avatar-fb-orb {
    position: relative;
    width: 170px; height: 170px;
    border-radius: 50%;
    background: radial-gradient(circle at 30% 30%,
      rgba(255, 255, 255, 0.4), rgba(232, 121, 249, 0.6) 30%,
      rgba(192, 132, 252, 0.9) 60%, rgba(125, 74, 208, 1));
    box-shadow:
      0 20px 60px rgba(192, 132, 252, 0.6),
      inset -20px -20px 60px rgba(74, 27, 138, 0.5);
    animation: sBreathe 3s ease-in-out infinite;
  }
  @keyframes sBreathe { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.05); } }
  #safiya-root .s-avatar-fb-face {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -40%);
    width: 100px; height: 60px;
  }
  #safiya-root .s-avatar-fb-eye {
    position: absolute; top: 0;
    width: 12px; height: 18px;
    background: #1a0a2e;
    border-radius: 50%;
    animation: sBlink 4s ease-in-out infinite;
  }
  #safiya-root .s-avatar-fb-eye-l { left: 15px; }
  #safiya-root .s-avatar-fb-eye-r { right: 15px; }
  @keyframes sBlink {
    0%, 92%, 100% { transform: scaleY(1); }
    94% { transform: scaleY(0.1); }
  }
  #safiya-root .s-avatar-fb-mouth {
    position: absolute; bottom: 5px; left: 50%;
    transform: translateX(-50%);
    width: 30px; height: 15px;
    border: 3px solid #1a0a2e;
    border-top: none;
    border-radius: 0 0 30px 30px;
  }
  #safiya-root .s-avatar-fb-name {
    font-weight: 800; font-size: 1.3rem;
    color: #e879f9;
    letter-spacing: 0.15em;
    text-shadow: 0 2px 20px rgba(232, 121, 249, 0.5);
  }
  #safiya-root .s-avatar-loader {
    position: absolute; inset: 0;
    display: none; align-items: center; justify-content: center;
    z-index: 4;
  }
  #safiya-root .s-avatar-spinner {
    width: 40px; height: 40px;
    border: 3px solid rgba(192, 132, 252, 0.2);
    border-top-color: #e879f9;
    border-right-color: #c084fc;
    border-radius: 50%;
    animation: sSpin 0.8s linear infinite;
  }
  @keyframes sSpin { to { transform: rotate(360deg); } }
  #safiya-root .s-avatar-hint {
    margin-top: 8px;
    background: #1a0f36;
    border: 1px solid rgba(232, 121, 249, 0.4);
    color: #f3e8ff;
    padding: 6px 14px;
    border-radius: 999px;
    font-size: 0.72rem; font-weight: 700;
    white-space: nowrap;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
    animation: sHint 2s ease-in-out infinite;
    pointer-events: none;
  }
  @keyframes sHint { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-3px); } }

  /* ═══════════════════════════════════════════════
     ★★★ CHAT PANEL — SOLID DARK, NO TRANSPARENCY ★★★
     ═══════════════════════════════════════════════ */
  #safiya-root .s-chat {
    position: fixed;
    bottom: 20px; right: 20px;
    z-index: 9998;
    width: 400px;
    height: 620px;
    max-height: calc(100vh - 40px);
    display: flex;
    flex-direction: column;
    /* SOLID DARK — works in light+dark mode */
    background: #0f0620 !important;
    border: 1px solid rgba(192, 132, 252, 0.3);
    border-radius: 24px;
    box-shadow:
      0 30px 80px rgba(0, 0, 0, 0.8),
      0 0 60px rgba(192, 132, 252, 0.25);
    overflow: hidden;
    opacity: 0; visibility: hidden;
    transform: translateY(20px) scale(0.95);
    transform-origin: bottom right;
    transition: all 0.45s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  #safiya-root .s-chat-open { opacity: 1; visibility: visible; transform: translateY(0) scale(1); }

  #safiya-root .s-chat-head {
    display: flex; align-items: center; gap: 10px;
    padding: 14px 16px;
    background: #1a0f36 !important;
    border-bottom: 1px solid rgba(192, 132, 252, 0.25);
    flex-shrink: 0;
  }
  #safiya-root .s-chat-btn {
    background: rgba(192, 132, 252, 0.12) !important;
    border: none;
    color: #c084fc !important;
    width: 34px; height: 34px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; transition: all 0.2s;
    -webkit-tap-highlight-color: transparent;
    padding: 0; flex-shrink: 0;
  }
  #safiya-root .s-chat-btn:hover {
    background: rgba(192, 132, 252, 0.25) !important;
    color: #e879f9 !important;
  }
  #safiya-root .s-chat-btn-close:hover {
    background: rgba(239, 68, 68, 0.25) !important;
    color: #ef4444 !important;
  }
  #safiya-root .s-chat-head-info { display: flex; align-items: center; gap: 10px; flex: 1; min-width: 0; }
  #safiya-root .s-chat-head-avatar {
    position: relative;
    width: 40px; height: 40px; border-radius: 50%;
    background: linear-gradient(135deg, #c084fc, #e879f9, #7dd3fc);
    display: flex; align-items: center; justify-content: center;
    color: #fff !important;
    flex-shrink: 0;
    box-shadow: 0 3px 10px rgba(192, 132, 252, 0.5);
    border: 2px solid rgba(232, 121, 249, 0.4);
    font-weight: 800; font-size: 1.1rem;
  }
  #safiya-root .s-chat-head-avatar-letter {
    color: #fff !important;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
    font-family: 'Bricolage Grotesque', sans-serif;
  }
  #safiya-root .s-chat-head-online {
    position: absolute; bottom: 0; right: 0;
    width: 10px; height: 10px;
    background: #22c55e;
    border: 2px solid #1a0f36;
    border-radius: 50%;
  }
  #safiya-root .s-chat-head-text { flex: 1; min-width: 0; }
  #safiya-root .s-chat-head-name {
    font-weight: 800; font-size: 1rem;
    color: #f3e8ff !important;
    line-height: 1.2;
  }
  #safiya-root .s-chat-head-status {
    font-size: 0.72rem;
    color: rgba(192, 132, 252, 0.9) !important;
    margin-top: 3px;
    font-weight: 500;
  }
  #safiya-root .s-chat-head-actions { display: flex; gap: 4px; flex-shrink: 0; }

  /* ═══════════════════════════════════════════════
     MESSAGES — GUARANTEED VISIBLE BUBBLES
     ═══════════════════════════════════════════════ */
  #safiya-root .s-chat-body {
    flex: 1; overflow-y: auto;
    padding: 18px 16px;
    display: flex; flex-direction: column;
    gap: 8px;
    background: #0f0620 !important;
    scrollbar-width: thin;
    scrollbar-color: rgba(192, 132, 252, 0.35) transparent;
    scroll-behavior: smooth;
  }
  #safiya-root .s-chat-body::-webkit-scrollbar { width: 5px; }
  #safiya-root .s-chat-body::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, #c084fc, #e879f9);
    border-radius: 3px;
  }
  #safiya-root .s-msg-time {
    text-align: center;
    font-size: 0.65rem;
    color: rgba(192, 132, 252, 0.6) !important;
    font-weight: 700;
    letter-spacing: 0.12em;
    margin: 14px 0 10px;
    text-transform: uppercase;
  }

  #safiya-root .s-msg-row {
    display: flex !important;
    align-items: flex-end !important;
    gap: 8px !important;
    margin: 2px 0 !important;
    animation: sMsgIn 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  @keyframes sMsgIn {
    from { opacity: 0; transform: translateY(8px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  #safiya-root .s-msg-her { justify-content: flex-start !important; }
  #safiya-root .s-msg-me  { justify-content: flex-end !important; }

  /* AVATAR CIRCLE - guaranteed visible */
  #safiya-root .s-msg-avatar {
    width: 28px !important;
    height: 28px !important;
    border-radius: 50% !important;
    background: radial-gradient(circle at 30% 30%, #f0abfc 0%, #c084fc 60%, #7c3aed 100%) !important;
    flex-shrink: 0 !important;
    box-shadow: 0 2px 8px rgba(192, 132, 252, 0.4) !important;
    border: 1.5px solid rgba(232, 121, 249, 0.6) !important;
  }

  /* ═══ BUBBLES — SOLID, BORDERED, VISIBLE ═══ */
  #safiya-root .s-msg-bubble {
    max-width: 72% !important;
    padding: 11px 16px !important;
    border-radius: 22px !important;
    font-size: 0.92rem !important;
    line-height: 1.45 !important;
    word-wrap: break-word !important;
    font-family: 'DM Sans', -apple-system, sans-serif !important;
    font-weight: 500 !important;
    overflow-wrap: break-word !important;
  }

  /* SAFIYA'S BUBBLE — dark purple background + purple border */
  #safiya-root .s-msg-her .s-msg-bubble {
    background: #2d1b4e !important;
    color: #f3e8ff !important;
    border: 1.5px solid rgba(192, 132, 252, 0.6) !important;
    border-bottom-left-radius: 6px !important;
    box-shadow: 0 3px 12px rgba(192, 132, 252, 0.2) !important;
  }

  /* USER'S BUBBLE — cyan-tinted background + cyan border */
  #safiya-root .s-msg-me .s-msg-bubble {
    background: #164e63 !important;
    color: #ecfeff !important;
    border: 1.5px solid rgba(125, 211, 252, 0.65) !important;
    border-bottom-right-radius: 6px !important;
    box-shadow: 0 3px 12px rgba(125, 211, 252, 0.25) !important;
  }

  #safiya-root .s-msg-typing {
    background: #2d1b4e !important;
    border: 1.5px solid rgba(192, 132, 252, 0.6) !important;
    padding: 13px 16px !important;
    border-radius: 22px !important;
    border-bottom-left-radius: 6px !important;
    display: flex !important;
    gap: 5px !important;
  }
  #safiya-root .s-msg-typing span {
    width: 7px; height: 7px;
    background: linear-gradient(135deg, #c084fc, #e879f9);
    border-radius: 50%;
    animation: sTBounce 1.3s ease-in-out infinite;
  }
  #safiya-root .s-msg-typing span:nth-child(2) { animation-delay: 0.15s; }
  #safiya-root .s-msg-typing span:nth-child(3) { animation-delay: 0.3s; }
  @keyframes sTBounce {
    0%, 60%, 100% { transform: translateY(0); opacity: 0.5; }
    30% { transform: translateY(-6px); opacity: 1; }
  }

  /* ═══ INPUT BAR ═══ */
  #safiya-root .s-chat-input-bar {
    display: flex; align-items: center; gap: 6px;
    padding: 12px 14px;
    background: #1a0f36 !important;
    border-top: 1px solid rgba(192, 132, 252, 0.25);
    flex-shrink: 0;
  }
  #safiya-root .s-input-icon {
    background: none; border: none;
    color: #c084fc !important;
    width: 34px; height: 34px;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; border-radius: 50%;
    padding: 0;
    -webkit-tap-highlight-color: transparent;
    flex-shrink: 0;
  }
  #safiya-root .s-input-icon:hover { background: rgba(192, 132, 252, 0.15); }
  #safiya-root .s-input-wrap {
    flex: 1;
    display: flex; align-items: center;
    background: #2d1b4e !important;
    border: 1.5px solid rgba(192, 132, 252, 0.3);
    border-radius: 22px;
    padding: 0 6px 0 16px;
    min-width: 0;
    transition: all 0.2s;
  }
  #safiya-root .s-input-wrap:focus-within {
    border-color: rgba(232, 121, 249, 0.6);
    box-shadow: 0 0 0 3px rgba(232, 121, 249, 0.15);
  }
  #safiya-root #s-input {
    flex: 1; background: none !important; border: none !important;
    color: #f3e8ff !important;
    font-family: 'DM Sans', sans-serif !important;
    font-size: 0.92rem;
    padding: 10px 0; outline: none;
    min-width: 0; font-weight: 500;
  }
  #safiya-root #s-input::placeholder { color: rgba(192, 132, 252, 0.55) !important; }
  #safiya-root .s-input-emoji {
    background: none; border: none;
    color: #c084fc !important;
    padding: 6px; cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    -webkit-tap-highlight-color: transparent;
    border-radius: 50%;
  }
  #safiya-root .s-send {
    background: linear-gradient(135deg, #c084fc, #e879f9, #7dd3fc) !important;
    border: none;
    color: #fff !important;
    width: 38px; height: 38px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; flex-shrink: 0;
    transition: all 0.3s;
    box-shadow: 0 4px 15px rgba(192, 132, 252, 0.5);
    -webkit-tap-highlight-color: transparent;
    padding: 0;
  }
  #safiya-root .s-send:hover { transform: scale(1.1); }

  /* ═══ MINIMIZED PILL ═══ */
  #safiya-root .s-mini {
    position: fixed;
    bottom: 20px; right: 20px;
    z-index: 9998;
    display: flex; align-items: center; gap: 10px;
    padding: 8px 14px 8px 8px;
    background: #1a0f36 !important;
    border: 1px solid rgba(232, 121, 249, 0.4);
    border-radius: 100px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    cursor: pointer;
    opacity: 0; visibility: hidden;
    transform: translateY(20px);
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  #safiya-root .s-mini-show { opacity: 1; visibility: visible; transform: translateY(0); }
  #safiya-root .s-mini-avatar {
    width: 34px; height: 34px; border-radius: 50%;
    background: linear-gradient(135deg, #c084fc, #e879f9, #7dd3fc);
    display: flex; align-items: center; justify-content: center;
    color: #fff !important;
    flex-shrink: 0;
    box-shadow: 0 2px 8px rgba(192, 132, 252, 0.4);
    font-weight: 800; font-size: 0.9rem;
  }
  #safiya-root .s-mini-name { font-weight: 700; color: #f3e8ff !important; font-size: 0.88rem; }
  #safiya-root .s-mini-new {
    min-width: 20px; height: 20px; padding: 0 6px;
    background: linear-gradient(135deg, #ef4444, #f97316);
    color: #fff !important; border-radius: 999px;
    display: none; align-items: center; justify-content: center;
    font-size: 0.65rem; font-weight: 800;
  }
  #safiya-root .s-mini-x {
    background: rgba(255, 255, 255, 0.05); border: none;
    color: rgba(243, 232, 255, 0.6) !important;
    width: 24px; height: 24px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    padding: 0;
  }

  /* ═══════════════════════════════════════════════
     TABLET (720-1024px)
     ═══════════════════════════════════════════════ */
  @media (max-width: 1024px) {
    #safiya-root .s-chat { width: 380px; }
    #safiya-root .s-avatar { right: 410px; }
  }

  /* ═══════════════════════════════════════════════
     MOBILE (< 720px) — NO AVATAR, chat only, right-anchored
     ═══════════════════════════════════════════════ */
  @media (max-width: 720px) {
    #safiya-root .s-head { bottom: 16px; right: 16px; width: 54px; height: 54px; }
    #safiya-root .s-bubble { bottom: 84px; right: 12px; left: 12px; width: auto; }

    /* Chat: sticks to right, takes most of screen, SOLID background */
    #safiya-root .s-chat {
      bottom: 12px !important;
      right: 12px !important;
      left: 12px !important;
      width: auto !important;
      height: 80vh !important;
      max-height: 80vh !important;
      /* EXTRA SOLID on mobile - no bleed-through */
      background: #0a0517 !important;
    }
    #safiya-root .s-chat-body {
      background: #0a0517 !important;
    }
    #safiya-root .s-chat-head {
      background: #1a0f36 !important;
    }
    #safiya-root .s-chat-input-bar {
      background: #1a0f36 !important;
    }

    /* HIDE avatar completely on mobile — too small screen for both */
    #safiya-root .s-avatar {
      display: none !important;
    }

    #safiya-root #s-input {
      font-size: 1rem;
      padding: 12px 0;
    }
    #safiya-root .s-send { width: 40px; height: 40px; }
    #safiya-root .s-mini { bottom: 14px; right: 14px; left: 14px; justify-content: flex-start; }
  }

  @media (prefers-reduced-motion: reduce) {
    #safiya-root .s-head-ring, #safiya-root .s-head-notify,
    #safiya-root .s-avatar-glow, #safiya-root .s-avatar-show,
    #safiya-root .s-avatar-fb-orb, #safiya-root .s-avatar-fb-eye,
    #safiya-root .s-avatar-hint {
      animation: none !important;
    }
  }
</style>
